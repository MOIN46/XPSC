void solve_the_problem(int test_case)
{  
    string s1, s2;
    cin >> s1 >> s2;
    int cnt = 0, l = s1.size();

    for (int i = 0; i < l; i++)
    {
        if (s1[i] != s2[i])
            cnt++;
    }

    cout << cnt << endl;
}
