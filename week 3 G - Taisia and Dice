void solve_the_problem(int test_case)
{
    int n, s, r;
    cin >> n >> s >> r;

    cout << s - r << " ";

    for (int i = 0; i < n - 1; i++)
    {
        cout << (r + i) / (n - 1) << " ";
    }

    cout << endl;
}
